# Hized Platform — Progress Log

Shared status file for AI coding agents (Claude Code, Codex, etc.) working on this repo.

## How to use this file

- **Before starting work**, read "Current State" below to understand what exists, what's in progress, and what's already been decided — especially the RLS notes, which record real bugs already found and fixed.
- **After your session**, add a new dated entry under "Session Log" (newest at top) summarizing what you did, which files/migrations changed, and what's next. A few sentences, not a full diff — the git history has the diff.
- **Update "Current State"** if your session changed the overall picture (a task completed, a new architecture decision, a new gap found).
- **After each completed task**, give the user a one-page CEO-style bullet summary: outcome, business value, evidence, an estimated completion percentage for the shared Hized Platform MVP and the marketing site, next plan and material pending items. Northstar and Harbour are synthetic demonstration tenants used to prove product journeys and isolation, not separate products with their own completion percentages. Treat percentages as delivery estimates rather than test coverage.
- **Don't silently remove or reverse another agent's entry or decision.** If you disagree, add a note explaining why rather than overwriting it — the next agent (or the human) needs to see both sides.
- If you're about to touch Row-Level Security policies, read the "RLS notes" section below first — several real cross-tenant data leaks were found and fixed here already, and the same bug shape is easy to reintroduce in a new table if the pattern isn't followed.

## Current State

**This repo** (`hized-platform`) is the actual multi-tenant Hized product. It is separate from `hized-web` (the marketing site, github.com/JCurrie8/Hized, deployed to hized.com) — do not confuse the two or edit one expecting it to affect the other.

**Stack**: Next.js (App Router, TypeScript) + Neon (Postgres) + Better Auth (self-hosted, no bundled vendor) + Cloudflare R2 (private object storage with direct signed browser upload) + Vercel. pnpm + Turborepo monorepo.

**Product spec**: [`docs/product/blueprint.md`](docs/product/blueprint.md) (v2.4) is the source of truth for scope and requirements — read it before assuming what a feature should do. It includes a Platform Administration section (7), MVP delivery phases (11.4), a shared governed visual grammar, and recipient-safe automated Pulse/Canvas report delivery plus Connect incident/recovery notifications.

**Deployment**:
- Marketing site → Vercel project `hized` → `hized.com` / `www.hized.com` (live and separate from this repo). Marketing PR #1 aligned the final-product story to blueprint v2.2, added the `hized.app` client entry and SEO discovery routes, and deployed as commit `b599b80` on 2026-08-03. The user confirmed on 2026-08-03 that no domain currently has email forwarding or mail-specific DNS records configured; do not assume MX/SPF/DKIM exists.
- This platform app → Vercel project `hized-platform`, git-linked to this repo. The canonical production domain is `hized.app`, with tenant apps on `*.hized.app`; the user moved the domain to Vercel nameservers on 2026-08-03. Production must use `BETTER_AUTH_URL=https://hized.app`, `COOKIE_DOMAIN=.hized.app`, and the `https://hized.app/api/connect/microsoft/callback` OAuth redirect. The legacy `hized-platform.vercel.app` URL remains a preview/fallback route; `MIGRATIONS_DATABASE_URL` is intentionally not in Vercel.

**Phase 0 (product foundation) progress** — blueprint section 11.4, this repo's own build sequence:

- [x] Repo scaffold, Hized design tokens ported into Tailwind
- [x] Neon + Better Auth + R2 provisioned
- [x] Core schema & migrations (`db/migrations/`; migrations 0001–0025 are applied to both production and `vercel-dev`. Migration 0025 passed the hosted migration/integration gate and was applied transactionally to production before the matching KPI-governance application release.)
- [x] RLS policies + helper functions (`packages/testing/src/rls.test.ts`)
- [x] Auth integration — invitation signup/acceptance is bound to the single-use token and invited account email, including an existing user joining another tenant; MFA (TOTP) plugin wired server-side but **not yet enforced or given an enrollment UI**
- [x] Tenant resolution & app shell — subdomain middleware (`apps/web/src/proxy.ts` — Next.js 16 renamed `middleware.ts`; must live under `src/` given this project uses `--src-dir`), authenticated organisation landing/selection, a membership-validated Vercel/localhost path fallback, and login/invite/platform-admin pages. Tenant entry now lands on `/home`, with correctly active Home/Pulse/Connect/Settings navigation and a consolidated Settings landing.
- [x] Org hierarchy CRUD + drill-down (half-open history, acyclic immediate edits, move cascades ltree paths to descendants, tested; scheduled/backdated mutation is deliberately not exposed yet)
- [x] Audit logging — writer + both viewer UIs (`/admin/audit`, `/platform-admin/audit`) built; privileged mutations and their events commit atomically, and cross-tenant platform reads are audited before returning
- [x] CI/CD merge gate — the workflow uses a dedicated Neon `ci` branch with separate owner/runtime secrets and a mutation guard. Protected `main` requires both hosted checks, an up-to-date branch, resolved conversations, and applies the rules to administrators. PR #1 passed and deployed to Vercel production. A gated staging/production schema-release path remains deliberately operator-driven per `docs/runbooks/ci-cd.md`.
- [~] Demo tenant seeding & EPIC-01 walkthrough — guarded/idempotent seed tooling and two production demo hierarchies exist; restricted-role verification proves 12/6 own nodes and zero cross-tenant nodes. Northstar login and tenant entry are verified in production after PR #2; accepting the existing Harbour invitation and checking the tenant switch remain.
- [~] Security hardening & Phase 0 exit review — the 2026-08-02 review findings are fixed and covered by integration tests; the broader exit review remains
- [~] EPIC-04/05 Connect vertical slice — tenant-scoped connector/pipeline/batch/run/checkpoint schema and RLS tests exist in migration `0015`; direct signed CSV/XLSX upload and the SharePoint/OneDrive workbook path share immutable R2 revisions, bounded parsing, landed/validated/curated persistence, duplicate suppression, failed-run visibility and retryable attempts. Microsoft OAuth with PKCE, tenant/connector-bound encrypted refresh tokens, selected workbook resolution and end-of-reconciliation Graph delta commits are implemented in `/admin/connect`. Migrations `0017`/`0018` plus a protected hourly GitHub Actions trigger add leased polling and analyst-configured mappings, load rules, versions and per-pipeline intervals without giving the worker raw tenant-table access. The production schema and `hized.app` deployment are live; Entra registration and matching Vercel/GitHub connector/cron secrets still need activation. Reusable CRM/API incremental planning contracts are present; after Microsoft live verification, move to the governed dataset/KPI/Pulse thin slice and add reference adapters around pilot demand rather than open-ended connector breadth.
- [~] EPIC-06/07 Pulse + Canvas visual foundation — migrations `0024`–`0027`, the scoped dashboard, governed catalogue, shared 18-type visual renderer, Company Admin/Analyst Pulse composition, member-created Canvas boards, responsive sizing/reordering and Northstar/Harbour published examples are live in production through PR #14. Every render resolves approved KPI data under the current viewer rather than storing resolved values in a layout. Targeted Canvas grants, layout versioning, filters/inspect-data and governed maps remain subsequent slices.
- [x] Company-wide access setup — blueprint v2.2 makes Pulse and Canvas available to every active tenant member once commercially entitled, while separating product entitlement, feature role and organisation-row scope. Company Admins can invite and manage members with a role, active/suspended status and one enforced primary scope; Division is a first-class hierarchy node. Migrations 0019–0021 and PR #7 are live on production and `vercel-dev`.
- [x] Tenant branding and product hub — Company Admins can privately draft, preview, publish and reset a validated PNG/WebP logo, accessible colours and curated typography; published branding is tenant-member readable and draft rows are Company Admin-only. Blueprint v2.2 and migration 0023 add explicit active/trial/locked Pulse, Connect and Canvas entitlements controlled only by Hized Platform Administration. Home shows locked products with “Find out more”; interactive routes, actions, OAuth callback and scheduled SharePoint work enforce the same status. Migrations 0022–0023 and PR #8 are live on production; `hized.app`, the Northstar wildcard route, favicon and Better Auth session endpoint passed post-deploy smoke tests.
- [~] Hized Platform Administration — the user's existing production profile is now the first global Platform Admin, bootstrapped atomically with an immutable platform-level audit event. `admin.hized.app` exposes the separately branded, audited all-tenant list, tenant creation and cross-tenant audit viewer. The next sprint must add tenant detail, product entitlement, initial Company Admin recovery, onboarding configuration and suspend/reactivate controls; silent user impersonation remains explicitly out of MVP scope.

**Known gaps / deliberately deferred (not oversights — don't "fix" without checking why first)**:
- MFA enrollment UI + enforcement — plugin exists, nothing requires it yet.
- No Playwright/e2e automation — verification so far has been real vitest integration tests against the live Neon database, plus manual browser checks. Adding proper e2e is welcome but hasn't been prioritized yet.
- Invitation email delivery is currently skipped — invite links are shown/copied in the admin UI (`/admin/users`), never emailed. This was an explicit user decision to avoid a paid dependency pre-revenue. Blueprint v2.3 still requires provider-neutral transactional email for the final product's scheduled reports and operational notifications; no provider or sending-domain setup has yet been implemented.
- Blueprint section 7.5: PLATFORM-005 (cross-tenant health aggregation) and PLATFORM-006 ("view as" impersonation) are explicitly out of scope for MVP.
- CI is an active required merge gate backed by its own persistent Neon `ci` branch. The CI database is intentionally disposable, while staging/production schema releases remain operator-gated; follow `docs/runbooks/ci-cd.md` before introducing a migration.
- Local owner/runtime URLs now point to the separate Neon `vercel-dev` branch, which has migrations 0001–0027 and its own restricted `app_user`. The production branch is no longer the default target for local integration tests.
- The first tabular parser intentionally accepts `.csv` and modern `.xlsx`; legacy binary `.xls`, encrypted workbooks, macros and multi-sheet mapping UI are not silently guessed. SharePoint/Forms and manual upload will use the same parser, with an explicit sheet selection where required.
- The current Microsoft UI monitors one workbook per connection; multiple connections are supported. Leased polling checks for due work hourly and pipelines can select an hourly-to-daily interval, but it remains inactive until the Entra/Vercel connector variables and matching Vercel `CRON_SECRET` / GitHub `PRODUCTION_CRON_SECRET` are released. Migration 0017 is live. Webhook prompts, folder discovery and non-default SharePoint library browsing are deliberate follow-ons; `docs/runbooks/microsoft-connect.md` records setup and blueprint v2.0 clarifies that Graph cannot recover every intermediate workbook content edit between observations.
- Platform Administration does not yet expose an entitlement editor. Migration 0023 deliberately makes tenant product writes Platform Admin-only and seeds the current released baseline; add an audited Platform Admin UI before commercial operators need to change subscriptions without an owner-run release operation.
- The visual foundation deliberately does not fake a map from organisation labels. Maps require a governed geography/dimension contract with validated identifiers or coordinates. Canvas currently supports private and whole-tenant publication; named-user/role/org-area sharing is represented in the database but its grant-management UI is the next sharing slice. Published layout version history, global/widget filters, inspect-data/drill-through, duplication/promotion and scheduled delivery are also pending.

**RLS notes — read before adding or changing a policy**:
- Tenant isolation is Postgres RLS, not Supabase's `auth.uid()` (this project isn't on Supabase — see blueprint's "Resolved since v1.1" note). Session context is two Postgres session variables (`app.current_user_id`, `app.current_tenant_id`) set per-transaction by `packages/db`'s `withUserContext()` — every authenticated query must go through it, or RLS sees no context and fails closed (visible as "nothing", not an error).
- The app connects as a restricted `app_user` Postgres role (`db/setup-app-role.mjs`), never as `neondb_owner` — that role has `BYPASSRLS=true` and would silently defeat every policy. Migrations use `MIGRATIONS_DATABASE_URL` (owner); the running app uses `DATABASE_URL` (`app_user`).
- **Three real cross-tenant leaks were found and fixed by testing, not by inspection** — the same bug shape is easy to reintroduce in a new table:
  - `db/migrations/0006`: `*_company_admin write` policies (`for all`, so also applied to `SELECT`) checked `is_company_admin(tenant_id)` against the *row's own* tenant, independent of session context — a company_admin of more than one tenant (e.g. Hized consultancy staff, supported by design) could read/write every tenant they administer at once, regardless of which tenant the session was scoped to. Fix: every such policy also requires `tenant_id = current_tenant_id()`.
  - `db/migrations/0007`: the `tenants` table's own SELECT policy queried `tenant_memberships` directly in a subquery — but RLS policies (unlike `SECURITY DEFINER` functions) run with the caller's own privileges, so that nested read was itself blocked by `tenant_memberships`' RLS when no tenant context was set yet (the literal "which tenants am I in" case). Fixed with a dedicated `SECURITY DEFINER` function, `current_user_tenant_ids()`.
  - `db/migrations/0011`: `audit_log`'s SELECT policy had the exact same shape as 0006, found by inspection before building the audit viewer UI on top of it — worth checking for this pattern in any table you add a "company_admin can manage their own tenant's X" policy to.
- Rule of thumb for a new tenant-scoped table's admin-write policy: `tenant_id = current_tenant_id() and (is_company_admin(tenant_id) or is_platform_admin())` — not just the role check alone.
- Migration `0013` hardens the surrounding database trust boundary: tenant context without a user is rejected in `withUserContext`, audit actors are session-bound, profile security columns are not runtime-updatable, invitation authority is token-bound, and Hized `SECURITY DEFINER` helpers use fixed search paths with no `PUBLIC` execute grant. Preserve those function and column grants in future migrations.
- A fourth isolation weakness was found by the EPIC-01 production verifier: a genuine profile paired by trusted server code with a tenant it did not belong to could read that tenant's `tenant_memberships`, `org_nodes`, and `invitations`, because those SELECT policies trusted `current_tenant_id()` without independently checking active membership. No client-controlled path around `getAuthContext()` was found, but migration `0014` now binds those reads to `current_user_has_tenant_access()` so a future server context-pairing bug fails closed at RLS too.
- Migration `0027` records a separate intra-tenant privacy correction caught before the visual release: KPI-governor authority initially made every private Canvas board readable/editable to Company Admins and Analysts. Canvas privacy is now ownership/explicit-grant based; KPI-governor access applies automatically only to Pulse. The live restricted-role test proves a Company Admin cannot open another member's private board.

## Session Log

### 2026-08-05 — Claude (Fable 5) (MFA enforcement — closing a Phase 0 decision)

**Why this and not new breadth:** a plan audit against blueprint 11.4 found four delivery phases open at once (Phase 0 still `[~]` on two items, Connect `[~]`, Pulse/Canvas `[~]`) while the blueprint's own build rule in 13.1 says "do not start the next epic until the current acceptance criteria pass". The clearest single casualty was MFA — an explicit Phase 0 decision by the user *and* a blueprint 9.3 requirement ("MFA and SSO for privileged users") — still sitting as a known gap 30 migrations later with the plugin registered but no UI, no route and no enforcement. Closed that rather than opening anything new.

Delivered: `mfa-policy.ts` (which roles require a second factor, plus the enrolment-page exemption), TOTP enrolment UI at `/admin/security` (tenant) and `/platform-admin/security`, a sign-in TOTP/backup-code challenge (`TwoFactorChallenge`) wired into `LoginForm` on Better Auth's `twoFactorRedirect`, and enforcement in both layouts. Company Admins are required; ordinary members are not (proportionality — the blueprint asks for privileged users specifically). Platform Admin is required unconditionally per blueprint 7.4.

One security detail worth carrying forward: layouts cannot read the pathname, so enforcement takes it from a header, and enforcement exempts the enrolment page by pathname. A client-supplied `x-pathname: /admin/security` would therefore have bypassed MFA on *every* page. `proxy.ts` now deletes and re-derives `x-pathname` exactly as it already did for `x-tenant-slug`, and sets it to the post-rewrite path. Two regression tests cover it.

Verified live against `vercel-dev` with a temporary unenrolled Company Admin (since removed): `/home` → `307 /admin/security`; the enrolment page itself → `200` with no redirect loop; a spoofed `x-pathname` header → still `307`. 118 web tests, typecheck, lint and production build all pass. Not done: enrolment is not yet exercised end-to-end with a real authenticator (Better Auth owns that verification), and there is still no Playwright coverage.

Next: Phase 0 genuinely has two items left — the EPIC-01 walkthrough tail (accept the Harbour invitation, check the tenant switch) and the Phase 0 exit review. Recommend closing those before further Connect/Pulse/Canvas breadth. Note both this branch and `docs/domain-and-email-corrections` touch PROGRESS.md, so whichever merges second needs a rebase.

### 2026-08-04 — Codex (Pulse/Canvas production release + initial Platform Admin)

Released the configurable Pulse and Canvas visual foundation through PR #14. After every hosted gate passed, applied migrations `0026_visual_dashboard_foundation.sql` and `0027_canvas_private_board_privacy.sql` transactionally to the verified Neon `production` branch, ran the guarded `visuals-v1` production seed, and verified both migration ledger entries, RLS on all four visual tables, all 16 policies, no `PUBLIC` execution on the visual authority helpers, and exactly two views in each demonstration tenant. Restricted `app_user` verification proved each seed principal still sees only its own tenant hierarchy/KPIs with zero cross-tenant nodes.

Production had no `platform_admins` row, explaining why the user's login only entered Northstar. Bootstrapped the existing `joshcurrie15@gmail.com` profile as the initial global Platform Admin without creating another login and wrote the immutable `platform_admin.bootstrap` audit event in the same transaction. PR #14 merged as `15208eb`; Vercel production deployment `dpl_2KiCzLdRjk9ERTm4XejMisg7AujW` reached READY. Live checks passed for `hized.app`, the Northstar wildcard, `admin.hized.app`, Better Auth session endpoint and favicon, and the one-hour runtime-error scan was clean. Next: build the Platform Admin tenant/entitlement/admin-recovery control centre, then continue visual filters/inspect-data, targeted sharing and duplication.

### 2026-08-04 — Codex (configurable Pulse + Canvas visual foundation release candidate)

Built the first shared visual/report object across Pulse and Canvas. Migrations `0026`–`0027` add tenant-bound views, widgets, governed KPI references and user/role/org/tenant grants with selected-tenant product gates, composite tenant foreign keys, immutable parent identity and separate read/write policies. A final policy review caught and corrected an intra-tenant privacy issue before production: KPI governors could initially see another user's private Canvas board. Canvas private content now requires ownership or an explicit grant; automatic governor authority is Pulse-only.

Added one responsive server-rendered visual engine with 18 types: KPI, line, area, column, horizontal bar, target attainment, donut, gauge, funnel, heatmap, table, text, combo, waterfall, treemap, radar, scatter and bullet. Semantic validation rejects misleading cardinality/unit combinations. Company Admins/Analysts can build and publish the tenant's default Pulse view; every entitled member can create a private Canvas board and publish it company-wide. The Home/navigation/Settings journeys expose Canvas and Pulse configuration, while layouts store configuration only and resolve KPI values through each viewer's own RLS context. Blueprint v2.4 makes that shared visual and data-authority contract explicit and defers maps until governed geography exists.

Northstar and Harbour development seeds now include published Pulse and Canvas examples. Migration `0026` and the privacy correction `0027` are applied to `vercel-dev`; the seed ran idempotently. Verification passes 102 web tests, 29 restricted-role RLS tests, five demo-manifest tests, monorepo typecheck, web lint, Next production build and an authenticated desktop/mobile browser journey with correct active navigation, zero horizontal overflow, no framework overlay and no console errors. The temporary review identity/invitations and local servers were removed. Next: release through hosted CI, apply `0026`–`0027` plus demo views to production, then build filters/inspect-data, targeted sharing and duplication before the scheduled-delivery service consumes published views.

### 2026-08-03 — Codex (Pulse hierarchy/trends + automated delivery blueprint)

Built the next customer-visible Pulse slice without adding a migration or new RLS policy: Company Admins can drill from company into permitted organisation children, scoped managers fall back safely to their primary branch when a requested node is inaccessible, and Pulse shows RLS-filtered breadcrumbs, governed green/amber/red threshold bands and up to six recent KPI points. A selected level with no value is explicit rather than silently receiving its parent's result. Batched queries avoid per-card or per-node N+1 work. The full 100-test web suite, 25 restricted-role RLS tests, typecheck, lint, Next.js production build and `git diff --check` pass; a real authenticated `vercel-dev` Company Admin journey verified company/North Region drill paths, desktop/mobile rendering, no horizontal overflow and a clean console. The temporary account, invitation, membership and audit row were removed afterward.

Updated the product specification to blueprint v2.3 for automated Pulse/Canvas report emails and Connect operational notifications. MVP delivery is limited to active tenant members, resolves every report independently under the recipient's current entitlement/role/organisation/data permissions, defaults to secure links, treats generated attachments as audited exports, and records idempotent attempts and provider outcomes. Connect notification requirements now cover failure, warning, stale source, schema drift, volume anomaly, retry exhaustion, escalation and recovery with deduplication. Transactional delivery is provider-neutral and requires authenticated sending-domain/bounce handling before production; external recipients and non-email channels remain later scope.

PR #12 passed the hosted quality/build, database/integration and Vercel preview gates, merged as `fc6f650` and deployed READY to Vercel production as `dpl_7EGuuPW88M62mc4M9yTLM2LMWyBu`. Post-deploy checks passed for the apex, Northstar tenant route, Better Auth session endpoint, favicon and SVG icon; the initial one-hour runtime-error scan was clean. Next: configurable Pulse dashboard/report composition, which creates the governed report object that scheduled delivery will consume.

### 2026-08-03 — Codex (marketing-site final-product alignment review)

Reviewed the separate live `hized-web` marketing site against blueprint v2.2 as a statement of the completed product, not the current implementation stage. Connect now reflects the normal multi-source SME estate and governed Hized storage; Pulse includes targets, alerts and owned actions; Canvas is explicitly role/scope-aware; the fourth pillar is aligned to the blueprint name Hized Advisory; the tenancy section describes database-enforced isolation and member scope without roadmap language; and navigation includes the `hized.app` client entry. Added canonical/Open Graph/Twitter metadata plus generated robots and sitemap routes, and suppressed the existing inline-style hydration mismatch on both the home and demo components. The Next.js production build, desktop/mobile browser checks, zero-overflow check, clean console and robots/sitemap responses pass. Marketing PR #1 merged as `b599b80`; Vercel production reached READY, live copy/metadata and 200 discovery routes were verified, and the one-hour production runtime-error scan was clean. The largest remaining marketing conversion gap is a real enquiry route: `#contact` previously led only to the footer, and the user has confirmed there is no domain email/forwarding setup, so no address or form backend was invented. Next platform task remains the Pulse hierarchy drill, trends and threshold presentation slice.

### 2026-08-03 — Codex (KPI governance workflow release candidate)

Built the next Pulse slice on `codex/kpi-governance-workflow`. Migration `0025_kpi_governance_workflow.sql` completes the minimum governed KPI contract with reviewer, target method and applicable organisation levels, prevents direct draft-to-approved updates, and exposes one narrow selected-tenant Company Admin approval function that atomically closes the previous approved version. The helper has a fixed empty search path, no `PUBLIC` execute grant and restricted runtime permission. Analysts can create/edit drafts; only Company Admins can approve or reject.

`/admin/kpis` now supports complete contract creation/editing, approval/rejection and drafting an effective-dated replacement version. Every mutation writes its audit event inside the same `withUserContext` transaction. A real authenticated `vercel-dev` browser journey created and approved a KPI successfully with no desktop overflow; its temporary user, invitation, KPI and audit records were then removed. Verification passes: 96 web tests, 25 uncached restricted-role RLS tests, four demo-manifest tests, monorepo lint/typecheck/build, `git diff --check`, PR #10's hosted quality/database gates and its Vercel preview. Migration 0025 was applied transactionally to production after those gates; production verification confirms the three columns, reviewer backfill/constraint, department default, private fixed-search-path approval helper, `app_user` execution and selected-tenant update policy. PR #10 merged at `a6ab83f`; Vercel reached READY, the apex/Northstar login redirects, auth session endpoint and icon passed live smoke checks, and the post-deploy runtime-error scan was clean. Next: hierarchy drill and KPI trend/threshold presentation rather than connector breadth.

### 2026-08-03 — Codex (production product hub release + governed Pulse foundation)

Verified PR #8 merged at `51c515f9` and Vercel completed the production deployment. Live smoke tests confirmed the apex and Northstar wildcard routes reach the authenticated product flow, the platform favicon is served, and Better Auth's session endpoint responds normally. This closes the tenant-branding/product-hub release described below.

Started the next customer-visible Pulse sprint on `codex/governed-kpi-pulse`. Migration `0024_governed_kpi_foundation.sql` adds published governed datasets and fields, immutable versioned KPI definitions, scoped KPI values and exact-tenant composite relationships. Its RLS requires the selected tenant, active membership, product audience and hierarchy scope; Analysts can author drafts but only a Company Admin can approve/reject them, and KPI value read/write policies are deliberately separate so analyst ingestion authority cannot widen read visibility. SECURITY DEFINER helpers use empty fixed search paths, schema-qualified operators and no PUBLIC execute grant. Northstar and Harbour seed data now demonstrate approved KPIs, target variance and stale source data, with restricted-role verification proving exact own-KPI counts and zero cross-tenant rows.

Pulse now reads the latest approved KPI at a member's primary organisation scope (company root for Company Admin), displaying actual/target variance, owner, period and data freshness. Company Admins and Analysts have a real read-only `/admin/kpis` catalogue. An authenticated browser pass caught a Postgres-date serialization bug that unit/type/build checks missed; SQL now returns canonical date text and timestamp epochs, with a regression assertion. A further RLS regression caught the `INSERT ... RETURNING` self-query snapshot edge case for draft definitions; the definition-row read policy now evaluates the candidate governance fields directly. The final browser pass showed both Northstar KPI cards, stale/off-target states, the catalogue, correct active Pulse navigation and no desktop overflow. The temporary review identity, invitations and audit rows were removed. Verification: 92 web tests, 25 restricted-role RLS tests, four demo tests, seed dry-run/transaction rehearsal, actual `vercel-dev` seed verification, monorepo lint/typecheck/build and browser checks pass. PR #9's quality build, Vercel preview and hosted migration/integration gate passed. Migration 0024 was then applied transactionally to production and verified as recorded with four RLS-enabled tables, 14 policies, private helper execution and `app_user` table privileges; production seed verification returned Harbour 1 dataset/1 KPI/3 values and Northstar 2/2/7. Application merge/deploy is next. After release, build the audited KPI create/version/approve workflow and hierarchy drill/trend surface rather than expanding connector breadth.

### 2026-08-03 — Codex (tenant branding, product hub and entitlements)

Built the approved tenant-customisation slice and the authenticated product landing requested immediately afterward. Migration `0022_tenant_branding.sql` separates private Company Admin drafts from published member-readable branding, validates logo metadata/format/dimensions and safe colour/typography choices, and keeps Platform Administration Hized-branded. The responsive Branding UI supports direct tenant-private R2 upload, preview, audited publish and reset; server-side Sharp decoding prevents extension/MIME-only image trust.

Blueprint v2.2 and migration `0023_tenant_product_entitlements.sql` establish explicit active/trial/locked access for Pulse, Connect and Canvas. Existing tenants retain Pulse/Connect while Canvas is discoverable and locked. Company Admins cannot self-enable commercial products; every RLS policy retains the selected-tenant predicate. Entitlements are enforced in navigation, typed Pulse/Connect routes, Connect server actions, Microsoft OAuth completion, direct SharePoint sync and the cross-tenant scheduled claimant. Tenant entry now lands on `/home`; the product hub separates subscription locks from role restrictions, provides “Find out more,” and `/admin` consolidates Organisation, Users, Branding and Audit. The platform now uses the marketing site's exact favicon, and route-derived navigation highlights Home, Pulse, Connect or Settings correctly on both subdomains and `/t/{slug}` fallback routes.

Verification on isolated `vercel-dev`: migration 0023 applied transactionally after correcting an ambiguous seed-column name caught by the first rolled-back run; 88 web tests, 23 restricted-role RLS tests, monorepo lint/typecheck/build and `git diff --check` pass. A temporary Company Admin tenant proved 390px/1440px rendering, no horizontal overflow, correct active-tab colour, equal desktop cards, favicon links and locked-product presentation. Locking Connect removed it from navigation and redirected a direct `/admin/connect` request to Home; the temporary tenant, account, invitation, audit data and local server were removed. PR #8's required quality/database jobs and Vercel preview passed. Migrations 0022–0023 were then applied transactionally to the verified Neon `production` branch: both ledger rows, RLS on all three new tables, nine policies, two tenants with six exact default entitlements and no missing defaults, the new-tenant trigger, scheduled Connect entitlement gate and restricted `app_user` table grant were verified before application merge. Next product sprint is governed datasets/KPI definitions and the first real Pulse metric/drill path.

### 2026-08-03 — Codex (company-wide access setup and Division)

Turned the user's all-company Pulse/Canvas decision into blueprint v2.1 and working Company Admin setup. Feature role and organisation scope are now explicit independent controls; the existing `employee` database role is presented as End user. Migrations `0019`–`0021` add Division, let a current-tenant Company Admin continue identifying suspended members without reopening the multi-tenant-admin leak shape, and enforce at most one primary scope while preserving additional dotted-line scopes. The `/admin/users` UI now creates scoped invitations and manages role, active/suspended status and primary company/division/department/team/employee scope. Non-admin users cannot be saved or invited without scope; self-demotion/suspension and loss of the final active Company Admin fail closed; every accepted change is audited in the same transaction.

Added seven Neon-backed access-management tests, including cross-tenant target/scope rejection, ordinary-manager write rejection and a multi-tenant admin proving that a suspended user from tenant B remains invisible in tenant A. The complete web suite passes 69 tests and the restricted-role suite passes 20; typecheck, lint, direct Next.js production build and `git diff --check` pass. A real temporary `vercel-dev` tenant verified mobile/desktop responsive rendering, scoped invitation creation and the last-admin error in a production-mode browser with no overflow, overlay or console error; the temporary tenant, auth identity, audit rows and server were removed afterward. The first CI attempt exposed legacy multiple-primary rows in the persistent disposable CI database, so migration 0021 was improved to preserve every scope while deterministically selecting the shallowest active one as primary. The rerun passed both hosted gates. Migrations 0019–0021 were then applied transactionally to Neon production and verified for ledger state, tenant-bound policy text, a valid partial unique index, Division constraint, zero duplicate primary memberships and retained RLS. Also corrected the prior false email-DNS note: the user confirmed there is no email forwarding or mail-record setup on any domain.

The user also approved tenant customisation through logos, colours and typography. Blueprint v2.1 now defines a safe Company Admin branding surface with Hized-managed logo storage, accessible colours, curated locally hosted typography and audited preview/publish/reset. That is the next admin-foundation build slice after this access release, before governed KPI publication.

### 2026-08-03 — Codex (first customer-facing Pulse home)

Removed the deployed Phase 0 dead end that appeared at `hized.app/`: apex roots now pass through authenticated organisation resolution and tenant-subdomain roots enter `/dashboard`. Replaced the tenant dashboard placeholder and mobile-overflowing header with the first responsive Hized Pulse surface. It shows real role/scope-constrained organisation coverage, setup progress, data confidence and authorised Connect pipeline health, while explicitly leaving KPI values empty until governed definitions are published. All tenant reads use `withUserContext`; ordinary roles do not gain Connect visibility.

Added proxy redirect coverage and Neon-backed Pulse summary tests, taking the web suite to 62 passing tests. Web typecheck/lint, direct Next.js production build, `git diff --check`, and a local browser pass of the apex → organisation resolution → login journey succeed with no error overlay or console error. Hosted CI and the Vercel preview passed; PR #6 was merged and production deployment `1d33abd` is live. Production smoke tests prove the apex redirects through `/organisations` to login and Northstar's root redirects through `/dashboard` to login for a signed-out request. The monorepo Turbo build produced its existing Windows link-name warning and exceeded the combined command timeout, but the direct web production build completed successfully. Next: migration 0019 for governed dataset/KPI definitions, then publish real demo KPI values and the first executive-to-team drill path into this home.

### 2026-08-03 — Codex (analyst pipeline builder + hized.app cutover)

Built the first analyst-operated pipeline configuration slice instead of adding another provider-specific form. Migration `0018_pipeline_configuration.sql` adds tenant-scoped field mappings and append-only configuration versions with the same single-complete-predicate RLS pattern as the other Connect tables. The guided `/admin/connect/pipelines/[pipelineId]` flow now covers observed source schema, included/renamed/typed fields, required checks, snapshot/append/upsert keys, Microsoft poll frequency and audited versioned saves. The ingestion engine actually applies that contract before curated upsert and quarantines required/type/key failures. The hourly production poll trigger now targets the canonical `hized.app` endpoint. Migration 0018 is applied only to `vercel-dev`; 58 web tests, 20 restricted-role RLS tests, monorepo typecheck/lint/build and `git diff --check` pass.

Updated the blueprint to v2.0: Company Admins authorise connections, analysts configure pipelines without seeing credentials, Salesforce/Zendesk are reference adapter shapes rather than an exhaustive named commitment, and Hized-managed tenant-isolated SQL is the default analytical destination for files/Forms/CRM/API data. Cut canonical platform routing, Better Auth origins, R2 CORS and Microsoft/demo runbooks to `hized.app` / `*.hized.app`; the production R2 bucket accepts the new origins, Vercel has valid production assignments for the apex and wildcard, and production `BETTER_AUTH_URL=https://hized.app` plus `COOKIE_DOMAIN=.hized.app` are active. Remaining release work is to register/set the Microsoft callback and connector/cron secrets. Historical `*.hized.com` session-log references below describe the old state and are superseded by this entry; the marketing site remains on `hized.com`.

Released through PR #5 after both CI jobs and the Vercel preview passed. Applied migrations 0017 and 0018 transactionally to the Neon `production` branch and verified the migration ledger, RLS enablement and policy presence before merging. Vercel deployed merge commit `9d3dac3` to production with `hized.app` and `*.hized.app` aliases; apex, Northstar tenant routing, login redirection and the unauthenticated Better Auth session endpoint passed live smoke tests. The Microsoft Entra/client/encryption/cron variables remain the only connector activation gate.

### 2026-08-03 — Codex (security-focused CTO pitch review)

Reviewed the current product blueprint, security invariants, restricted-role RLS suite, authentication boundary, connector credential encryption, private R2 upload controls and CI/CD gate to prepare an evidence-led CTO pitch. No product code changed. The recommended positioning distinguishes controls already implemented and tested from pre-pilot assurance gaps: MFA enforcement/SSO, rate limiting and file malware scanning, observability/incident response, tested backup/restore and retention/offboarding, privacy assessment, and independent security testing.

### 2026-08-03 — Codex (Microsoft workbook connector runnable path)

Extended the SharePoint delta planner into a runnable Connect path without changing the already-tenant-scoped migration 0015 schema. Added multi-tenant Microsoft authorization-code OAuth with PKCE, authenticated-encrypted refresh tokens bound to tenant + connector, token refresh rotation, connected-account/workbook configuration, SharePoint default-library and OneDrive path resolution, bounded Graph download, direct immutable R2 storage, the common CSV/XLSX load/run path, source item/path/eTag/cTag lineage, selected deletion/error state and manual Sync now UI. All authenticated database work stays inside `withUserContext`; Microsoft/Graph/R2 network calls stay outside those short transactions; a delta checkpoint commits only after its complete reconciliation and successful load, so replay uses the existing content/run deduplication contract.

Updated the blueprint to v1.9 and added `docs/runbooks/microsoft-connect.md`. Important correction rather than a silent assumption: Graph delta exposes the latest observed drive state, not downloadable content for every edit between polls, so Hized records every revision it observes and relies on cumulative Forms upsert to retain intervening responses; it must not claim unobserved intermediate workbook versions. Current UI intentionally handles one selected workbook per connection and on-demand sync. Next: create/configure the Entra app, release migration 0017 plus matching Vercel/GitHub secrets, and live-test against a real Forms workbook before moving to Salesforce/Zendesk live extraction. Verification passes 56 web tests, 19 restricted-role RLS tests, 3 demo-manifest tests, monorepo typecheck/lint/build and `git diff --check`.

Continued with the multiple-updates-per-day requirement: migration `0017_sharepoint_poll_leases.sql` adds configurable poll timing, ten-minute leases and a fixed `claim_due_sharepoint_syncs()` SECURITY DEFINER function. The function returns only due job IDs plus an active Connect operator; each actual sync still re-enters ordinary user/tenant RLS, and optimistic starting-delta checks prevent an expired/stale worker from regressing a newer cursor. Added a timing-safe `CRON_SECRET` endpoint and an hourly GitHub Actions trigger (inactive until matching production secrets exist). Migration 0017 is applied only to `vercel-dev`; production migration + secrets must precede merge/deployment.

### 2026-08-02 — Codex (SharePoint delta reconciliation started)

After releasing PR #4, created `codex/sharepoint-ingestion` from the production merge and started the next Connect source. Added a pure Microsoft Graph drive-delta planner that treats Graph item IDs as authoritative, collapses repeated item appearances to the documented last occurrence, permits checkpoints only on the final `@odata.deltaLink`, retains selected deletion state, limits downloads to the shared CSV/XLSX parser, validates timestamps/sizes, and rejects continuation URLs outside the Microsoft Graph v1.0 origin. Four focused tests and web typecheck pass. Next: Microsoft OAuth/credential encryption, site/drive/item selection, and the server-side delta/download runner that hashes each workbook revision into the existing batch/run pipeline; webhooks remain prompts to reconcile rather than a source of truth.

### 2026-08-02 — Codex (direct file ingestion + SME/Custom ETL boundary)

Completed the first end-to-end Connect run path. Company admins and analysts can create multiple independent file pipelines, upload bounded CSV/XLSX files directly to private R2 with a short-lived signed PUT, and finalise them through tenant-scoped server actions. R2 retains signed tenant/pipeline/hash metadata; the server verifies metadata, byte length and a recomputed SHA-256 before parsing. Snapshot, append and key-based upsert modes now persist immutable source batches, landed rows, quarantined-key validations, curated records, step status, row counts and audited completion/failure events. Unchanged file revisions are idempotent and redundant exact R2 objects are removed. The production bucket CORS policy is reproducible and live preflight tests pass for the Vercel origin and `*.hized.com` tenant origins.

Found and corrected a run-model bug in migration 0015: `unique (pipeline_id, source_batch_id)` made the declared retry trigger impossible and could make a failed upload look like a successful duplicate. Migration `0016_allow_pipeline_run_retries.sql` removes that constraint, adds the lookup index, and the application serialises concurrent batch processing with a transaction advisory lock while deduplicating only completed runs. Migration 0016 is applied and verified on both `vercel-dev` and production Neon. Updated the blueprint to v1.8 and Connect UI to treat SME multi-source data as normal and to distinguish paid Hized-managed Custom ETL from standard connectors without promising a self-service join/code studio. Verification passes 44 web tests, 19 RLS tests, migration tests, typecheck, lint and the production build. Next build sequence: SharePoint/Forms revision ingestion, Salesforce/Zendesk provider auth/extraction, then governed datasets/KPIs and a real Northstar Pulse dashboard.

### 2026-08-02 — Codex (Connect files + CRM foundation)

Started EPIC-04/05 on branch `codex/connect-file-ingestion`. Updated the blueprint to v1.7: SharePoint/OneDrive Excel and Forms workbooks are revision-aware first-class sources; Salesforce and Zendesk are first-class CRM adapters on a shared checkpoint/batch/run contract, with a persisted high-water mark plus a configurable 24-hour overlap for the existing Salesforce daily-upsert pattern. Added migration `0015_connect_file_pipeline_foundation.sql` and applied it to `vercel-dev`: all Connect relationships use composite tenant foreign keys, a run cannot pair a pipeline and batch from different connectors, unchanged content is deduplicated, CRM tombstones are retained, and one complete RLS predicate per table avoids the permissive-policy OR leak shape. The real restricted-role suite now passes 19 RLS tests, including multi-membership, mismatched-context, employee-denial, deduplication and connector-link attacks.

Built shared validated Salesforce/Zendesk incremental planning contracts, including `SystemModstamp` windows, stable `Id` ordering/upsert semantics, and end-of-stream-only Zendesk cursor commits. Added tenant-safe internal navigation and the first real `/admin/connect` page: connector inventory plus an audited action that creates a manual CSV/XLSX pipeline and checkpoint transactionally. Added a common bounded CSV/XLSX parser for both manual and SharePoint revisions, covering Forms-style rows, sheet selection, dates, formula results and hostile/duplicate headers. Verification passes 35 web tests, 19 RLS tests, contracts/web typecheck, and the Next production build. PR #3 passed both protected CI checks and its Vercel preview; migration 0015 was then applied transactionally to the production Neon branch and verified recorded with the Connect tables present. Next is direct R2 upload/finalisation plus landed/validated/curated run persistence, followed by Graph delta sync and live Salesforce/Zendesk authentication/extraction.

### 2026-08-02 — Codex (protected production gate + EPIC-01 walkthrough)

Completed GitHub sudo approval, saved the rotated restricted CI database secret, and reran PR #1. All four checks passed, including `Quality and production build` and `Migrations and database integration tests`; PR #1 merged to protected `main` at `fb7bbfa`, Vercel reported the corresponding production deployment READY, and the live platform returned 200. Configured `main` to require pull requests, both CI checks, an up-to-date branch, resolved review conversations, and no administrator bypass.

Created real company-admin invitations for the user's account in both Northstar Installations and Harbour Field Services. Opened the Northstar acceptance page for the user to choose their own application password; after acceptance, the remaining EPIC-01 work is to verify both authenticated tenant views and role boundaries. Created `codex/connect-vertical-slice` from the released `main`; the next product build is the blueprint's connector → ETL run/validation → curated job → Pulse KPI thin vertical slice rather than another open-ended hardening pass.

The Northstar signup returned 200 in production, but its client redirect sent the authenticated user to `/dashboard` on the apex Vercel hostname, which cannot supply a tenant slug and therefore correctly rendered the app shell's `Access denied` state. Added an authenticated `/organisations` landing route, single/multi-membership resolution through the existing user-only RLS context, and an apex-only `/t/:slug/*` rewrite for Vercel/localhost; canonical `*.hized.com` links remain unchanged. Login and both invitation acceptance paths now enter through organisation resolution, and a missing tenant slug recovers there instead of presenting a misleading denial. Updated the blueprint to v1.5 with the organisation-selection and preview-routing requirement. Verification passes 40 tests (14 RLS, 23 web, 3 demo-manifest), typecheck, lint, production build, and `git diff --check`.

### 2026-08-02 — Codex (CI activation)

Provisioned a persistent Neon `ci` branch cloned from production, configured GitHub's `CI_MIGRATIONS_DATABASE_URL` / `CI_DATABASE_URL` secrets and `CI_DATABASE_SAFE_TO_MUTATE=true` guard, and confirmed the restricted runtime role sees each demo tenant's own hierarchy with zero cross-tenant rows. The first hosted database job correctly failed closed before configuration; after activation, its safety gate and cumulative migrations passed and exposed two test-path issues rather than a database isolation failure.

Added `MIGRATIONS_DATABASE_URL` to Turborepo's environment allowlist so GitHub-supplied owner credentials reach Vitest, and disambiguated the shared invitation fixture parameter's Postgres type. Verification against the isolated CI branch now passes all 35 tests (14 RLS, 18 web, 3 demo-manifest), plus lint, typecheck, production build, and `git diff --check`. Next: merge through a green pull request, require both checks on protected `main`, then finish real admin invitations and the interactive EPIC-01 walkthrough.

Also migrated the existing Neon `vercel-dev` branch through 0014, created its own restricted `app_user`, switched both ignored local environment files away from production, and reran all 35 tests successfully there. The CI role was rotated and its exact connection verified locally; updating GitHub's `CI_DATABASE_URL` is currently paused at GitHub's sudo confirmation because the mobile approval request timed out. PR #1 remains open and unmerged until that secret is saved and both hosted checks pass.

### 2026-08-02 — Codex (GPT-5 production release + EPIC-01 seed)

Released the security-hardening work to `main` and Vercel production after applying migration `0013`, rotating the Neon `app_user` password and Better Auth secret, and configuring production-only Vercel runtime variables. Commit `c975550` reached READY at `https://hized-platform.vercel.app`; its build had no missing-environment warning, the homepage and a short-lived real Neon invitation both returned 200, the probe tenant was deleted, and the deployment had no warning/error/fatal runtime logs.

Started the next task: added guarded, idempotent Phase 0 demo seeding, manifest tests, a restricted-role isolation verifier, and `docs/runbooks/demo-tenants.md`; seeded Northstar Installations and Harbour Field Services into production without any real-person login. The first verifier run uncovered a context/membership gap in three RLS SELECT policies. Added and applied migration `0014_bind_tenant_context_to_membership.sql` plus a regression test; the post-migration verifier now proves each seed principal sees exactly its own tenant and 12/6 own hierarchy nodes with zero cross-tenant rows. Updated the blueprint to v1.4 to make synthetic demo delivery progressive and to forbid development/CI mutation of the production Neon branch. Next: provision separate Neon development/CI branches and GitHub gates, then issue real admin invitations and complete the interactive EPIC-01 walkthrough.

### 2026-08-02 — Codex (GPT-5 implementation)

Implemented every actionable finding from the preceding review. Migration `0013_security_hardening.sql` was applied to development: invitations now require the stored token hash and resolve the Better Auth account email in the database, existing users can accept an invitation to another tenant, hierarchy scope ranges are consistently half-open, audit actors and profile-column privileges are constrained, and all Hized `SECURITY DEFINER` helpers have fixed search paths and explicit grants. `withUserContext` now rejects tenant-only context and the proxy strips untrusted incoming tenant headers.

Organisation moves now lock the affected tree, reject self/descendant parents, cascade paths with scope assertions, and permit only immediate interactive edits until scheduled/backdated reorganisation semantics are designed (initial imports can still establish history). Privileged mutations and reads now write audit events in the same transaction. Updated the product blueprint to v1.3 with these invariants, added the GitHub Actions CI workflow plus `docs/runbooks/ci-cd.md`, and retained Vercel Git integration as the sole deployment path. Verification passed: 31 integration tests (13 RLS + 18 web), typecheck, lint, production build, and `git diff --check`. CI activation still requires the dedicated Neon branch/secrets/guard variable, protected-main settings, and a gated staging/production migration-release path described in the runbook; next product task after that is demo tenant seeding and the EPIC-01 walkthrough.

### 2026-08-02 — Codex (GPT-5 review)

Security/correctness review only; no production code changed. Read the v1.2 blueprint and reviewed migrations 0003/0006/0007/0011, `withUserContext`, `getAuthContext`, org hierarchy operations/tests, proxy routing, and the relevant auth/audit call sites. The three recorded RLS fixes are sound in the final cumulative schema, the live runtime role is correctly non-superuser/non-`BYPASSRLS`, and the existing focused suites passed (8 RLS tests + 11 web tests; typecheck also passed).

Disagreements / follow-ups found: (1) invite acceptance is not bound to the secret token or verified email ownership — Better Auth signup accepts anyone who knows a pending invited email, and the existing auth test actually exercises signup with an arbitrary unused token hash; treat this as the highest-priority security issue. (2) `editOrgNode` permits moving a node under its own descendant and persists a parent cycle; a temporary live integration probe confirmed it. (3) `current_user_scope_paths()` treats `valid_to` as inclusive (`>=`) while the schema/list queries use an exclusive end, so a move effective today returns both old and new scope paths; also confirmed live. (4) `/platform-admin/audit` reads cross-tenant data without logging that view, contrary to PLATFORM-003/section 7.4, and privileged mutations generally commit in a separate transaction from their audit write, so an audit failure can leave an unaudited successful action. (5) `profiles: update self only` restricts rows but not columns, so the broad `app_user` UPDATE grant permits changing security-sensitive `auth_user_id` and `is_hized_staff` if a self-profile update path is added. (6) `withUserContext` accepts `tenantId` with a null/unvalidated `userId` (an invalid state that tenant-only SELECT policies would honor), `proxy.ts` preserves a client-supplied `x-tenant-slug` on non-tenant hosts, and the live `SECURITY DEFINER` helpers still grant EXECUTE to `PUBLIC`; none has a current HTTP exploit path found, but all weaken the intended trust boundary and should be hardened during the Phase 0 security pass.

### 2026-08-02 — Claude (Sonnet 5)

Created this file. Summarized all prior work above into "Current State" so a cold-started agent (or Codex) has the full picture, not just today's diff.

Completed task 8 (audit logging): writer wired into invite creation, org node create/deactivate, invitation acceptance (via the `accept_invitation_by_email` SQL function, since it runs before any session context exists), and platform-admin's tenant-list view (PLATFORM-003). Built both viewer UIs (`/admin/audit` tenant-scoped, `/platform-admin/audit` cross-tenant) and tenant provisioning (PLATFORM-001) and invite-creation UIs, neither of which existed before — invitations could previously only be created via raw SQL in tests, and tenants only via a scratch script.

Three more real bugs found by testing live rather than assumed away:
- `db/migrations/0011`: `audit_log`'s own SELECT policy had the same "company_admin of multiple tenants sees more than they should" leak as 0006 — caught by inspection before building the viewer, not after.
- `db/migrations/0012`: `profiles.full_name` was never populated from Better Auth's captured name, so the audit viewer showed "system" instead of the actual actor — caught by looking at the live viewer output, not by code review.
- `apps/web/src/proxy.ts`: the admin-subdomain rewrite only handled the exact root path ("/"), so `/platform-admin/audit` was unreachable via `admin.*/audit` — a 404 in the browser is what surfaced this. Fixed to a prefix-exclusion (skip `/login`, `/invite`, `/api`; rewrite everything else) instead of an exact-match inclusion.

All three fixes verified live end-to-end: invite → signup → org node create → tenant create, checked against both audit viewers, confirmed correct actor names and correct tenant scoping (including cross-tenant visibility for the platform-admin view specifically).

Also handled, unrelated to task 8: sent the user a marketing+platform setup overview PDF and confirmed the blueprint was current at that time; DNS/wildcard-domain work for `*.hized.com` was paused. **Correction (2026-08-03):** the user confirmed there is no email forwarding or mail-record setup on any domain, so the earlier MX/SPF statement was inaccurate and must not be used as deployment guidance.

Next: CI/CD pipeline (task 9).
